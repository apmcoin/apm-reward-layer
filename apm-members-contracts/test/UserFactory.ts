import { expect } from "chai";
import { ethers } from "hardhat";
import { UserFactory } from './../typechain-types/UserFactory';

describe('PlusMemberManager', () => {
  // let UserFactoryFactory: any;

  // let user: any;
  // let userFactory: UserFactory;
  
  // before(async () => {
  //   [[user], UserFactoryFactory] = await Promise.all([
  //     ethers.getSigners(), 
  //     ethers.getContractFactory("UserFactory"),
  //   ]);
  // })

  // beforeEach(async () => {
  //   userFactory = await UserFactoryFactory.deploy();
  // });

  // it('통과한다', async () => {
  // });
})